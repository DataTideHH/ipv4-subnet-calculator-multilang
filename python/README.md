# Python Implementation

The Python 3.12 implementation will be added in Phase 3.

Planned characteristics:

- Python standard library only
- no installation step beyond a compatible interpreter
- interactive and direct command-line modes
- explicit validation
- dataclass-based calculation result
- behavior aligned with `docs/behavior-specification.md`
