# C++ Implementation

The C++20 implementation will be added in Phase 2.

Planned characteristics:

- CMake-based build
- no external runtime dependencies
- interactive and direct command-line modes
- explicit validation
- structured calculation result
- behavior aligned with `docs/behavior-specification.md`
